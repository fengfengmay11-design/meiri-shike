# 每日时刻项目协作规范

## 项目目标

“每日时刻”是一个帮助用户根据身心状态、健康目标、饮食偏好和所在校园选择餐食的产品。

项目包含两个运行端：

- 根目录 `index.html`：网页版 Demo，用于快速预览和产品验证。
- `miniprogram/`：微信小程序，是计划正式发布的主要产品。

## 项目结构

- `index.html`：网页版入口。
- `miniprogram/pages/`：微信小程序页面。
- `miniprogram/utils/`：推荐算法、菜谱、学校数据、存储和 AI 客户端逻辑。
- `miniprogram/cloudfunctions/`：微信云函数。
- `docs/`：PRD、算法和项目文档。
- `screenshots/`：产品截图。
- `brand/`：品牌图片和头像。

## 安全规则

- 禁止在代码、文档、测试文件或提交记录中写入真实 API Key、Secret、Token 或密码。
- 小程序前端不得保存真实 AI API Key。
- AI 服务密钥必须放在微信云函数环境变量中。
- 环境变量名称可以提交，真实值不得提交。
- 修改认证、用户健康数据、云函数或外部 API 前，必须说明安全影响。
- 提交前搜索疑似密钥和敏感信息。
- 发现凭证泄露时，先在服务商后台停用或删除，再修改代码。

## Git 工作流

- 不直接在 `main` 分支开发。
- 每个需求创建独立分支。
- 分支命名使用：
  - `feature/功能名称`
  - `fix/问题名称`
  - `security/安全问题`
  - `docs/文档名称`
  - `refactor/重构名称`
- 修改完成后先检查差异，再提交。
- 通过 Pull Request 合并到 `main`。
- PR 合并后删除临时分支，并同步本地 `main`。

## 开发原则

- 修改前先阅读相关代码和文档。
- 优先进行范围最小、容易验证的修改。
- 不修改与当前任务无关的文件。
- 不在修复功能时顺便进行大规模重构。
- 涉及推荐算法时，说明规则、输入、输出和降级行为。
- 网页版与小程序存在相同功能时，检查两端是否需要保持一致。
- AI 调用失败时，应保留清晰、可用的降级体验。
- 健康和营养建议不得表述为医疗诊断。

## 验证要求

每次修改完成后必须说明：

1. 修改了哪些文件。
2. 每个修改解决了什么问题。
3. 如何进行本地验证。
4. 哪些内容尚未验证。
5. 是否影响网页版。
6. 是否影响微信小程序。
7. 是否涉及环境变量、云函数或外部服务。
8. 是否发现安全风险。

## Codex 工作要求

- 开始修改前，先总结对任务和代码现状的理解。
- 需求存在歧义时，先指出关键假设。
- 未经明确要求，不执行发布、上传、合并或删除操作。
- 不声称测试通过，除非实际执行了对应测试。
- 无法运行微信开发者工具时，应明确说明需要人工完成的验证。
- 修改完成后提供简洁的变更摘要和验证结果。